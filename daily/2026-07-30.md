# Daily — 2026-07-30

_Updated 2026-07-30 by Codex after the KSM host and real-agent acceptance._

## Current task

**→ Kanban Surface Manager (M20) — first vertical slice accepted; prepare the
collab-mem projection contract.**

## What's being worked on / very next step

**Next step:** implement and test the smallest collab-mem projection against
`projects/kanban-surface.md`, with Git remaining durable and KSM remaining the
single execution-state writer. Do not add MCP or public ingress.

## Today's list

| id | Item | Ties | Owner | Status |
|----|------|------|-------|--------|
| D1 | Archive and strip `alwyzon-1` to a hardened KSM MVP host | M20 | Codex | ✅ done |
| D2 | Install/auth isolated Hermes planner and Codex worker; prove real ACP | M20 | Codex | ✅ done |
| D3 | Deploy and harden the KSM/KRP daemon; verify live worker lifecycle | M20 | Codex | ✅ done |
| D4 | Run `KSM-PILOT-1`: Hermes plan → reviewed Codex patch → GitHub PR | M20 | Codex | ✅ done — PR #2 merged; release accepted; card `DONE` |
| D5 | Project verified state into collab-mem and push the ledger | M20 | Codex | ✅ done |

## Done today

- Verified a recoverable local shelf archive before purging approved legacy VPS
  state; recorded the archive path and SHA-256 in FACTS.
- Reduced the host to SSH-only ingress and separate locked service identities.
- Proved KRP locally and in the real systemd service; rejected the first release
  when the worker could read SQLite, fixed the group boundary, and retested.
- Authenticated Hermes and Codex under separate homes and completed real ACP
  turns without workspace mutation.
- Merged KSM foundation PR #1 and deployed its accepted immutable service
  release.
- Proved cancellation/rollback behavior for an over-budget plan, a malformed
  diff, and a patch rejected at the human gate.
- Completed Tony's strict vanilla-host pass: removed the installed
  Docker/containerd stack, Snap/LXD, cron, cloud/provisioning agents,
  PackageKit/Polkit, irrelevant hardware/storage daemons, VMware tools on KVM,
  stale identities, pilot/rejected-release artifacts and caches.
- Booted the stripped host twice. Final acceptance showed only SSH/42 public,
  KSM healthy, no failed units or orphan packages, 367 MiB RAM used, and
  224 GiB free.
- Re-tested real ACP after cleanup: exact `HERMES_ACP_SMOKE_OK` and
  `CODEX_ACP_SMOKE_OK`, both `end_turn`, both workspaces unchanged.

## Handoff

- KSM records `KSM-PILOT-1` as `DONE`, revision 15, with no active lease.
- Hermes' accepted 1,288-byte plan is stored in KSM events.
- Codex's reviewed patch passed 15 tests, merged through PR #2, and is deployed
  as immutable release `9b98356249e4fbdd0c447e7e0930abb0e6d70df5`.
- The live health result is deterministic; database hash and mtime were
  unchanged, and the worker still cannot read canonical state.
- Host baseline is now strict: no container runtime, cron scheduler,
  cloud-init/Ubuntu management agent or loose application daemon remains.

## Blockers / operator decisions

- None for the current pilot.
- Public ingress, MCP exposure and the final surface UI remain intentionally
  deferred until the collab-mem projection and restart-recovery tests pass.
